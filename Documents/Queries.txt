CREATE TABLE REQUEST (RQ_SYSTEM_ID VARCHAR(50),RQ_TOKEN_ID INT);

CREATE TABLE PROCESS_INFO_AGGR (
RQ_TOKEN_ID INT,
PI_TIME_STAMP TIMESTAMP,
)

CREATE TABLE PROCESS_INFO_AGGR(
RQ_TOKEN_ID INT,
PI_TIME_STAMP TIMESTAMP,
PI_PROCESS_ID INT,
PI_PROCESS_NAME VARCHAR(50),
PI_PPID INT(10),
PI_MEM_USAGE INT(6),
PI_RAM INT(6),
PI_CLOCK_SPEED FLOAT(2),
PI_THREAD INT(6),
PI_CACHE INT(6));

CREATE TABLE CPU_INFO_AGGR(
RQ_TOKEN_ID INT,
CI_TIME_STAMP TIMESTAMP,
CI_IP_ADD VARCHAR(50),
CI_NUM_PROC INT(4),
CI_MEM_SIZE INT(6),
CI_RAM INT(6),
CI_CLOCK_SPEED FLOAT(3),
CI_THREAD INT(6),
CI_CACHE VARCHAR(50));

CREATE PROCESS_INFO_LIVE(
RQ_TOKEN_ID INT,
PIL_TIME_STAMP TIMESTAMP,
PIL_PROCESS_ID INT,
PIL_MEM_USAGE INT(6),
PIL_RAM INT(6),
PIL_CLOCK_SPEED FLOAT(3),
PIL_THREAD INT(6),
PIL_CACHE INT(6));

CREATE CPU_INFO_LIVE(
CI_TOKEN_ID INT,
CIL_TIME_STAMP TIMESTAMP,
CIL_IP_ADD VARCHAR(50),
CIL_NUM_PROC INT,
CIL_MEM_SIZE INT(6),
CIL_RAM INT(6),
CIL_CLOCK_SPEED FLOAT(3),
CIL_THREAD INT(6),
CIL_CACHE INT(6)
);


create table sysanatomy.cpu_info(
  
cpu_idle_time varchar(50),
  
cpu_nice_time varchar(50),
 
 cpu_invol_wait_time varchar(50),
  
cpu_kernel_time varchar(50),
  
cpu_time varchar(50),
  
cpu_user_time varchar(50),
  
cpu_wait_time varchar(50),
 
 cpu_cache varchar(50),
  
cpu_vendor_id varchar(50),
 
 cpu_cores varchar(50),
 
 cpu_sockets varchar(50),
 
 cpu_csocket varchar
cpu_speed varchar(50),
 
 cpu_model varchar(50));